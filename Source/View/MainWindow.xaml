<Window x:Class="Fluxogrammer.Source.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Fluxogrammer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">

        
    <Window.Resources>
        <Style x:Key="BotaoMenu" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Height="450" Width="600">
        <Grid.Background>
            <LinearGradientBrush StartPoint="1,0" EndPoint="0,1">
                <GradientStop Color="#5B1094" Offset="0"/>
                <GradientStop Color="#708AEF" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Canvas>
            <Border Canvas.Top="20" Canvas.Left="200" HorizontalAlignment="Center">
                <Label FontSize="20" Height="34" Width="200" HorizontalAlignment="Center" VerticalAlignment="Center">
                    Teste de menu inicial
                </Label>
            </Border>

            <Border Canvas.Top="100" Canvas.Left="0" HorizontalAlignment="Center" Width="600">
                <Canvas>
                    <Button Content="Novo Fluxograma" Height="34" HorizontalAlignment="Center" 
                    VerticalAlignment="Center" Canvas.Left="100" Style="{StaticResource BotaoMenu}"
                    Padding="10,5" x:Name="NewButton"/>

                    <Button Content="Carregar Fluxograma" Height="34" HorizontalAlignment="Center" 
                    VerticalAlignment="Center" Canvas.Right="100" Style="{StaticResource BotaoMenu}"
                    Padding="10,5" x:Name="LoadButton"/>
                </Canvas>
            </Border>
        </Canvas>
    </Grid>
</Window>
